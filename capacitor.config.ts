import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vercel.senbox',
  appName: 'senbox',
  webDir: 'out',
};

export default config;

// out -> dùng cho nextjs
// public -> dùng cho Vue, Angular
